<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.stockm8.mapper.UserMapper">

	<!-- 회원가입 -->
	<insert id="insertUser">
		insert into users(email, password, name, role,
		tel_number)
		values(#{email}, #{password}, #{name}, #{role}, #{telNumber})
	</insert>

	<!-- 로그인 체크 -->
	<select id="loginCheck" resultType="com.stockm8.domain.vo.UserVO">
		select * from users
		where
		user_id=#{userId} and userpw=#{password}
	</select>

	<!-- 회원정보 조회 -->
	<select id="getUser" resultType="com.stockm8.domain.vo.UserVO">
		select * from users
		where
		user_id = #{userId}
	</select>

	<!-- 회원정보 수정 -->
	<update id="updateUser">
		update users set
		username=#{username},updatedate=now()
		where userid=#{userid} and userpw=#{userpw}
	</update>

	<!-- 회원정보 삭제 -->
	<delete id="deleteUser">
		delete from users
		where userid=#{userid} and
		userpw=#{userpw}
	</delete>

	<!-- 회원 목록 조회 -->
	<select id="userList" resultType="com.stockm8.domain.vo.UserVO">
		select * from users
	</select>


</mapper>