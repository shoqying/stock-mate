<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockm8.mappers.receivingMapper">

	<!-- 오늘 입고될 리스트 -->
	<select id="getTodayReceivingList"
		resultType="com.stockm8.domain.vo.ReceivingShipmentVO">
		select *
		from tbl_receiving
		where Date(receivingDate) = curdate()
		order by receivingDate
		limit 0, 10
	</select>

	<!-- 어제 입고된 리스트 -->
	<select id="getYesterdayReceivingList"
		resultType="com.stockm8.domain.vo.ReceivingShipmentVO">
		select *
		from tbl_receiving
		where Date(receivingDate) = curdate() - INTERVAL 1 DAY
		order by receivingDate
		limit 0, 10
	</select>

</mapper>